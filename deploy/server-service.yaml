apiVersion: v1
kind: Service
metadata:
  labels:
    service: server
  name: server
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    service: server
status:
  loadBalancer: {}
